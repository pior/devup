dependencies:
  override:
    - pip install -U pip
    - pip install -e .
    - pip install -r requirements-dev.txt

test:
  override:
    - py.test --cov=devup tests/
    - flake8 devup
    - flake8 tests
  post:
    - codecov
